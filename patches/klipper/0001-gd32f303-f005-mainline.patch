diff --git a/src/generic/armcm_link.lds.S b/src/generic/armcm_link.lds.S
index 94dd2100d..f747e9e48 100644
--- a/src/generic/armcm_link.lds.S
+++ b/src/generic/armcm_link.lds.S
@@ -22,6 +22,13 @@ SECTIONS
         _text_vectortable_start = .;
         KEEP(*(.vector_table))
         _text_vectortable_end = .;
+#if CONFIG_MACH_GD32F303
+        /* Reserve the F005 updater board-information region. */
+        . = _text_vectortable_start + 0x200;
+        _f005_board_info_start = .;
+        . = . + 0x20;
+        _f005_board_info_end = .;
+#endif
         *(.text .text.*)
         *(.rodata .rodata*)
     } > rom
diff --git a/src/stm32/Kconfig b/src/stm32/Kconfig
index da1b47442..b80c3c306 100644
--- a/src/stm32/Kconfig
+++ b/src/stm32/Kconfig
@@ -7,14 +7,14 @@ config STM32_SELECT
     default y
     select HAVE_GPIO
     select HAVE_GPIO_ADC
-    select HAVE_GPIO_I2C if !MACH_STM32F031
-    select HAVE_GPIO_SPI if !MACH_STM32F031
+    select HAVE_GPIO_I2C if !MACH_STM32F031 && !MACH_GD32F303
+    select HAVE_GPIO_SPI if !MACH_STM32F031 && !MACH_GD32F303
     select HAVE_GPIO_SDIO if MACH_STM32F4
-    select HAVE_GPIO_HARD_PWM if MACH_STM32F042 || MACH_STM32F070 || MACH_STM32F072 || MACH_STM32F1 || MACH_STM32F4 || MACH_STM32F7 || MACH_STM32G0 || MACH_STM32G4 || MACH_STM32H7
+    select HAVE_GPIO_HARD_PWM if !MACH_GD32F303 && (MACH_STM32F042 || MACH_STM32F070 || MACH_STM32F072 || MACH_STM32F1 || MACH_STM32F4 || MACH_STM32F7 || MACH_STM32G0 || MACH_STM32G4 || MACH_STM32H7)
     select HAVE_STRICT_TIMING
     select HAVE_CHIPID
     select HAVE_STEPPER_OPTIMIZED_BOTH_EDGE if !MACH_STM32H7
-    select HAVE_BOOTLOADER_REQUEST
+    select HAVE_BOOTLOADER_REQUEST if !MACH_GD32F303
     select HAVE_LIMITED_CODE_SIZE if FLASH_SIZE < 0x10000
 
 config BOARD_DIRECTORY
@@ -31,6 +31,9 @@ choice
     config MACH_STM32F103
         bool "STM32F103"
         select MACH_STM32F1
+    config MACH_GD32F303
+        bool "GD32F303xE"
+        select MACH_STM32F1
     config MACH_STM32F207
         bool "STM32F207"
         select MACH_STM32F2
@@ -152,7 +155,7 @@ config MACH_N32G45x
 config HAVE_STM32_USBFS
     bool
     default y if MACH_STM32F0x2 || MACH_STM32G0Bx || MACH_STM32L4 || MACH_STM32G4
-    default y if (MACH_STM32F1 || MACH_STM32F070) && !STM32_CLOCK_REF_INTERNAL
+    default y if (MACH_STM32F1 || MACH_STM32F070) && !STM32_CLOCK_REF_INTERNAL && !MACH_GD32F303
 config HAVE_STM32_USBOTG
     bool
     default y if MACH_STM32F2 || MACH_STM32F4 || MACH_STM32F7 || MACH_STM32H7
@@ -161,7 +164,7 @@ config STM32_USB_DOUBLE_BUFFER_TX
     default y
 config HAVE_STM32_CANBUS
     bool
-    default y if MACH_STM32F1 || MACH_STM32F2 || MACH_STM32F4x5 || MACH_STM32F446 || MACH_STM32F0x2
+    default y if (MACH_STM32F1 && !MACH_GD32F303) || MACH_STM32F2 || MACH_STM32F4x5 || MACH_STM32F446 || MACH_STM32F0x2
 config HAVE_STM32_FDCANBUS
     bool
     default y if MACH_STM32G0B1 || MACH_STM32H7 || MACH_STM32G4
@@ -179,6 +182,7 @@ config MCU
     default "stm32f070xb" if MACH_STM32F070
     default "stm32f072xb" if MACH_STM32F072
     default "stm32f103xe" if MACH_STM32F103
+    default "gd32f303xe" if MACH_GD32F303
     default "stm32f207xx" if MACH_STM32F207
     default "stm32f401xc" if MACH_STM32F401
     default "stm32f405xx" if MACH_STM32F405
@@ -203,6 +207,7 @@ config CLOCK_FREQ
     default 48000000 if MACH_STM32F0
     default 64000000 if MACH_STM32F103 && STM32_CLOCK_REF_INTERNAL
     default 72000000 if MACH_STM32F103
+    default 120000000 if MACH_GD32F303
     default 120000000 if MACH_STM32F207
     default 84000000 if MACH_STM32F401
     default 168000000 if MACH_STM32F4x5
@@ -220,6 +225,7 @@ config CLOCK_FREQ
 
 config FLASH_SIZE
     hex
+    default 0x3d000 if MACH_GD32F303
     default 0x8000 if MACH_STM32F031 || MACH_STM32F042 || MACH_STM32F070x6 || MACH_STM32F103x6
     default 0x20000 if (MACH_STM32F070 || MACH_STM32F072) && !MACH_STM32F070x6
     default 0x10000 if (MACH_STM32F103 && !MACH_STM32F103x6) || MACH_STM32L412 # Flash size of stm32f103x8 (64KiB)
@@ -241,6 +247,7 @@ config RAM_START
 
 config RAM_SIZE
     hex
+    default 0x10000 if MACH_GD32F303
     default 0x1000 if MACH_STM32F031
     default 0x1800 if MACH_STM32F042
     default 0x1800 if MACH_STM32F070x6
@@ -289,20 +296,22 @@ config STM32_DFU_ROM_ADDRESS
 
 choice
     prompt "Bootloader offset"
+    config STM32_FLASH_START_3000
+        bool "12KiB bootloader" if MACH_GD32F303
     config STM32_FLASH_START_2000
-        bool "8KiB bootloader" if MACH_STM32F1 || MACH_STM32F070 || MACH_STM32G0 || MACH_STM32G4 || MACH_STM32F0x2
+        bool "8KiB bootloader" if (MACH_STM32F1 && !MACH_GD32F303) || MACH_STM32F070 || MACH_STM32G0 || MACH_STM32G4 || MACH_STM32F0x2
     config STM32_FLASH_START_5000
         bool "20KiB bootloader" if MACH_STM32F103
     config STM32_FLASH_START_7000
-        bool "28KiB bootloader" if MACH_STM32F1
+        bool "28KiB bootloader" if MACH_STM32F1 && !MACH_GD32F303
     config STM32_FLASH_START_8000
-        bool "32KiB bootloader" if MACH_STM32F1 || MACH_STM32F2 || MACH_STM32F4 || MACH_STM32F7 || MACH_STM32H750
+        bool "32KiB bootloader" if (MACH_STM32F1 && !MACH_GD32F303) || MACH_STM32F2 || MACH_STM32F4 || MACH_STM32F7 || MACH_STM32H750
     config STM32_FLASH_START_8800
         bool "34KiB bootloader" if MACH_STM32F103
     config STM32_FLASH_START_20200
         bool "128KiB bootloader with 512 byte offset" if MACH_STM32F4x5
     config STM32_FLASH_START_9000
-        bool "36KiB bootloader" if MACH_STM32F1
+        bool "36KiB bootloader" if MACH_STM32F1 && !MACH_GD32F303
     config STM32_FLASH_START_C000
         bool "48KiB bootloader" if MACH_STM32F4x5 || MACH_STM32F401
     config STM32_FLASH_START_10000
@@ -311,20 +320,21 @@ choice
     config STM32_FLASH_START_800
         bool "2KiB bootloader" if MACH_STM32F103
     config STM32_FLASH_START_1000
-        bool "4KiB bootloader" if MACH_STM32F1 || MACH_STM32F0
+        bool "4KiB bootloader" if (MACH_STM32F1 && !MACH_GD32F303) || MACH_STM32F0
     config STM32_FLASH_START_4000
         bool "16KiB bootloader" if MACH_STM32F207 || MACH_STM32F401 || MACH_STM32F4x5 || MACH_STM32F103 || MACH_STM32F072
     config STM32_FLASH_START_20000
         bool "128KiB bootloader" if MACH_STM32H743 || MACH_STM32H723 || MACH_STM32F7 || MACH_STM32H750
 
     config STM32_FLASH_START_0000
-        bool "No bootloader"
+        bool "No bootloader" if !MACH_GD32F303
 endchoice
 config FLASH_APPLICATION_ADDRESS
     hex
     default 0x8000800 if STM32_FLASH_START_800
     default 0x8001000 if STM32_FLASH_START_1000
     default 0x8002000 if STM32_FLASH_START_2000
+    default 0x8003000 if STM32_FLASH_START_3000
     default 0x8004000 if STM32_FLASH_START_4000
     default 0x8005000 if STM32_FLASH_START_5000
     default 0x8007000 if STM32_FLASH_START_7000
diff --git a/src/stm32/Makefile b/src/stm32/Makefile
index 3c158d518..4b20ca5c5 100644
--- a/src/stm32/Makefile
+++ b/src/stm32/Makefile
@@ -21,6 +21,7 @@ MCU_UPPER := $(shell echo $(CONFIG_MCU) | tr a-z A-Z | tr X x)
 CFLAGS-$(CONFIG_HAVE_LIMITED_CODE_SIZE) += -Os
 CFLAGS-$(CONFIG_MACH_STM32F0) += -mcpu=cortex-m0 -Ilib/stm32f0/include
 CFLAGS-$(CONFIG_MACH_STM32F103) += -mcpu=cortex-m3
+CFLAGS-$(CONFIG_MACH_GD32F303) += -mcpu=cortex-m3 -DSTM32F103xE
 CFLAGS-$(CONFIG_MACH_N32G45x) += -mcpu=cortex-m4 -Ilib/n32g45x/include
 CFLAGS-$(CONFIG_MACH_STM32F1) += -Ilib/stm32f1/include
 CFLAGS-$(CONFIG_MACH_STM32F2) += -mcpu=cortex-m3 -Ilib/stm32f2/include
diff --git a/src/stm32/stm32f1.c b/src/stm32/stm32f1.c
index 94cfe1390..835689402 100644
--- a/src/stm32/stm32f1.c
+++ b/src/stm32/stm32f1.c
@@ -19,6 +19,13 @@
 
 #define FREQ_PERIPH (CONFIG_CLOCK_FREQ / 2)
 
+#define GD32_RCC_CFGR_PLLMF_4 (1U << 27)
+#define GD32_PWR_CR_LDOVS_HIGH (3U << 14)
+#define GD32_PWR_CR_HDEN (1U << 16)
+#define GD32_PWR_CR_HDS (1U << 17)
+#define GD32_PWR_CSR_HDRF (1U << 16)
+#define GD32_PWR_CSR_HDSRF (1U << 17)
+
 // Map a peripheral address to its enable bits
 struct cline
 lookup_clock_line(uint32_t periph_base)
@@ -59,7 +66,16 @@ clock_setup(void)
 {
     // Configure and enable PLL
     uint32_t cfgr;
-    if (!CONFIG_STM32_CLOCK_REF_INTERNAL) {
+    if (CONFIG_MACH_GD32F303) {
+        // The GD32F303 uses (8Mhz / 2) * 30 and GD-specific high drive.
+        RCC->CR |= RCC_CR_HSEON;
+        while (!(RCC->CR & RCC_CR_HSERDY))
+            ;
+        RCC->APB1ENR |= RCC_APB1ENR_PWREN;
+        PWR->CR |= GD32_PWR_CR_LDOVS_HIGH;
+        cfgr = (RCC_CFGR_PLLSRC | RCC_CFGR_PLLXTPRE_HSE_DIV2
+                | (13 << RCC_CFGR_PLLMULL_Pos) | GD32_RCC_CFGR_PLLMF_4);
+    } else if (!CONFIG_STM32_CLOCK_REF_INTERNAL) {
         // Configure 72Mhz PLL from external crystal (HSE)
         RCC->CR |= RCC_CR_HSEON;
         uint32_t div = CONFIG_CLOCK_FREQ / (CONFIG_CLOCK_REF_FREQ / 2);
@@ -75,17 +91,31 @@ clock_setup(void)
         cfgr = ((0 << RCC_CFGR_PLLSRC_Pos)
                 | ((div2 - 2) << RCC_CFGR_PLLMULL_Pos));
     }
-    cfgr |= RCC_CFGR_PPRE1_DIV2 | RCC_CFGR_PPRE2_DIV2 | RCC_CFGR_ADCPRE_DIV8;
+    cfgr |= RCC_CFGR_PPRE1_DIV2 | RCC_CFGR_PPRE2_DIV2;
+    if (CONFIG_MACH_GD32F303)
+        cfgr |= RCC_CFGR_ADCPRE_DIV6;
+    else
+        cfgr |= RCC_CFGR_ADCPRE_DIV8;
     RCC->CFGR = cfgr;
     RCC->CR |= RCC_CR_PLLON;
 
     // Set flash latency
-    FLASH->ACR = (2 << FLASH_ACR_LATENCY_Pos) | FLASH_ACR_PRFTBE;
+    if (!CONFIG_MACH_GD32F303)
+        FLASH->ACR = (2 << FLASH_ACR_LATENCY_Pos) | FLASH_ACR_PRFTBE;
 
     // Wait for PLL lock
     while (!(RCC->CR & RCC_CR_PLLRDY))
         ;
 
+    if (CONFIG_MACH_GD32F303) {
+        PWR->CR |= GD32_PWR_CR_HDEN;
+        while (!(PWR->CSR & GD32_PWR_CSR_HDRF))
+            ;
+        PWR->CR |= GD32_PWR_CR_HDS;
+        while (!(PWR->CSR & GD32_PWR_CSR_HDSRF))
+            ;
+    }
+
     // Switch system clock to PLL
     RCC->CFGR = cfgr | RCC_CFGR_SW_PLL;
     while ((RCC->CFGR & RCC_CFGR_SWS_Msk) != RCC_CFGR_SWS_PLL)
